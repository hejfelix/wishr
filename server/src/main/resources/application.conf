{

  dbSettings = {
    dataSourceClassName = "org.h2.jdbcx.JdbcDataSource",
    dataSource = {
      url = "jdbc:h2:mem:yourdbname"
      user = "sa"
    }
  },

  wishrSettings = {

    rootPath = "127.0.0.1:8080",

    databaseConfigKey = "dbSettings",

    emailSettings = {
      smtp: ${?WISHR_EMAIL_SMTP}"",
      port: 42,
      user: ${?WISHR_EMAIL_USER}"",
      password: ${?WISHR_EMAIL_PASSWORD}"",
      sender: ${?WISHR_EMAIL_SENDER}""
    },

    emailSettings.port = ${?WISHR_EMAIL_PORT}

  }
}
